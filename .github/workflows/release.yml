# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 lituus-lab
name: Release

on:
  push:
    tags: ['v*']

# Least privilege by default: guard/test only check the tag and run tests.
# The publish job widens its own scope to cut the release.
permissions:
  contents: read

jobs:
  guard:
    name: Tag matches .nimble version
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }
      # A tag disagreeing with the package version ships a release whose
      # embedded version lies. Refuse before anything is published.
      - run: |
          set -eu
          tag="${GITHUB_REF_NAME#v}"
          declared=$(grep -m1 '^version' UniGraph.nimble | cut -d'"' -f2)
          declared_py=$(grep -m1 '^version' py/pyproject.toml | cut -d'"' -f2)
          echo "tag=$tag declared=$declared declared_py=$declared_py"
          test "$tag" = "$declared"
          test "$tag" = "$declared_py"

  test:
    name: Nim tests (${{ matrix.os }})
    needs: guard
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }
      - uses: jiro4989/setup-nim-action@v2
        with:
          nim-version: '2.2.10'
      - run: nimble install -y
      - run: nimble testCi
      - run: nimble testCiRelease

  cabi:
    name: C ABI (${{ matrix.os }})
    needs: guard
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }
      - uses: jiro4989/setup-nim-action@v2
        with:
          nim-version: '2.2.10'
      - run: nimble install -y
      - run: nimble clibStatic
      - run: nimble clib
      - name: Package
        shell: bash
        run: |
          set -eu
          dir="UniGraph-${GITHUB_REF_NAME}-${{ matrix.os }}"
          mkdir -p "$dir/include"
          cp include/UniGraph.h "$dir/include/"
          cp libUniGraph.* "$dir/"
          cp LICENSE NOTICE "$dir/"
          tar czf "$dir.tar.gz" "$dir"
      - uses: actions/upload-artifact@v7
        with:
          name: cabi-${{ matrix.os }}
          path: '*.tar.gz'

  wheels:
    name: Wheel (${{ matrix.os }}, py${{ matrix.python-version }})
    needs: guard
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        # No Py_LIMITED_API/abi3 in setup.py: the extension is tied to each
        # CPython minor version's C ABI, so every one needs its own wheel.
        python-version: ['3.9', '3.10', '3.11', '3.12', '3.13', '3.14']
    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }
      - uses: jiro4989/setup-nim-action@v2
        with:
          nim-version: '2.2.10'
      - run: nimble install -y
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install -U setuptools wheel Cython pytest
      - uses: ilammy/msvc-dev-cmd@v1
        if: runner.os == 'Windows'
      - run: nimble pyLib
      # Build the extension in-place and run the test suite before packaging
      # a wheel — a release wheel that fails its own tests is worse than no
      # wheel. bdist_wheel (non-inplace) is built separately afterwards.
      - run: cd py && python setup.py build_ext --inplace
      - run: cd py && python -m pytest -q
      - run: cd py && python setup.py bdist_wheel
      - name: Repair Wheel
        shell: bash
        run: |
          set -eu
          case "$RUNNER_OS" in
            Linux)   pip install auditwheel && LD_LIBRARY_PATH="$GITHUB_WORKSPACE" auditwheel repair py/dist/*.whl -w dist/ ;;
            macOS)   pip install delocate   && DYLD_LIBRARY_PATH="$GITHUB_WORKSPACE" delocate-wheel py/dist/*.whl -w dist/ ;;
            Windows) pip install delvewheel && delvewheel repair py/dist/*.whl -w dist/ ;;
          esac
      - uses: actions/upload-artifact@v7
        with:
          name: wheel-${{ matrix.os }}-py${{ matrix.python-version }}
          path: dist/*.whl

  sdist:
    name: Source distribution
    needs: guard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { persist-credentials: false }
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install -U setuptools wheel Cython
      # Vendors the Nim source into py/_nimsrc/ (setup.py's vendor_nim_source),
      # so `pip install --no-binary` can build it on a platform/Python version
      # with no prebuilt wheel -- nimble/nim must be on the installer's PATH.
      - run: cd py && python setup.py sdist
      - uses: actions/upload-artifact@v7
        with:
          name: sdist
          path: py/dist/*.tar.gz

  publish:
    name: Publish
    needs: [test, cabi, wheels, sdist]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      - run: ls -R artifacts
      # Use the gh CLI instead of a third-party action: one fewer supply-chain
      # dependency, and `--generate-notes` produces the same auto-generated
      # release notes the softprops action did.
      - name: Create release with artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -eu
          find artifacts -type f -print0 \
            | xargs -0 gh release create "$GITHUB_REF_NAME" \
                --generate-notes --title "$GITHUB_REF_NAME"

  publish-pypi:
    name: Publish to PyPI
    needs: [wheels, sdist]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/unigraph/
    permissions:
      # Trusted publishing (OIDC): no API token stored here. Requires a
      # one-time manual step on pypi.org -- add lituus-lab/UniGraph,
      # workflow release.yml, environment pypi as a trusted publisher for
      # the unigraph project.
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: wheel-*
          path: dist
          merge-multiple: true
      - uses: actions/download-artifact@v4
        with:
          name: sdist
          path: dist
      - run: ls dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
          # This tag can re-run after a version already has some files on
          # PyPI (e.g. adding a Python version or the sdist later) -- PyPI
          # rejects re-uploading an unchanged filename, so skip rather than
          # fail the whole job on files already published.
          skip-existing: true
